// Puts quotes around *str*.
// Uses the quote-type (single/double) that would appear around *str* in-game when displaying it.

struct In {
    str: string,
}

struct Out {
    result: string
}

type Return = 1